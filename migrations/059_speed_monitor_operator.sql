-- 059: speed_monitor.operator — оператор симки на момент замера
-- Оператор может меняться между часами (перерегистрация в сети), поэтому
-- храним его в каждой строке, а не справочно — график стабильности строится
-- в разрезе «какой оператор на какой локации»
-- NB: комментарии без точек с запятой — раннер миграций наивно сплитит файл

ALTER TABLE speed_monitor ADD COLUMN operator TEXT NOT NULL DEFAULT '';
