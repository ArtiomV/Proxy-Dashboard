-- 053: bills.formula — разбор расчёта суммы счёта (2026-08-02).
-- По требованию оператора: на странице актов видно, из чего алгоритм исходил
-- при выставлении счёта (max(прошлый месяц, прогноз run-rate×1.1) + долг).

ALTER TABLE bills ADD COLUMN formula TEXT NOT NULL DEFAULT '';
